# Copy this file to .env and adjust if your backend isn't on the default port.
# Every API call goes through this — see src/api/client.ts. Vite only exposes
# env vars prefixed VITE_ to client-side code.
VITE_API_BASE_URL=http://localhost:8000
